#include <iostream>
#include "define.hxx"

int main(int argc, char *argv[]) {
    LamportsLogicalClock processes[DEFAULT_PROC_NUM];

    do {
        std::cout << "\nTotal procs: " << DEFAULT_PROC_NUM;
    } while(true);
    
    return 0;
}